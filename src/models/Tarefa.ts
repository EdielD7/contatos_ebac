class Tarefa {
  nome: string
  email: string
  id: number

  constructor(nome: string, email: string, id: number) {
    this.nome = nome
    this.email = email
    this.id = id
  }
}

export default Tarefa
